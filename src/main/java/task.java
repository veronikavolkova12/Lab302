public Task() {
        }

public class Task(String title, LocalDate dueDate, boolean status) {
        this.title = title;
        this.dueDate = dueDate;
        this.status = status;
}

public class ong getId() {
        return id;
        }

public class void setId(Long id) {
        this.id = id;
        }

public class String getTitle() {
        return title;
        }

public class void setTitle(String title) {
        this.title = title;
        }

public class LocalDate getDueDate() {
        return dueDate;
        }

public class void setDueDate(LocalDate dueDate) {
        this.dueDate = dueDate;
        }

public boolean isStatus() {
        return status;
        }

public void setStatus(boolean status) {
        this.status = status;
        }
        }
